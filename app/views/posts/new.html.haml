= provide(:title, "新規投稿")

%main.main__container
  .content-title__container
    %h2.content-title 飲みたい時間と場所を投稿しよう！
  .form__post-new
    = form_for @post, :html => {class: 'post_form'} do |f|
      .form-item
        = f.label "飲みたい時間", class: 'form-label'
        .form-select__container
          - current_datetime = @post.datetime
          - if current_datetime != nil
            - current_datetime = current_datetime.utc
          = f.select :datetime, options_for_select(datetime_options, current_datetime), {}, {class: 'form-select'}
      .form-item
        = f.label "飲みたい場所", class: 'form-label'
        = f.text_field :location, class: 'form-control'
      .form-item.form-button
        = f.submit "投稿する", class: 'button'
