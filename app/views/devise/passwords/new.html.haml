= provide(:title, "新規登録")

%main.main__container
  .content-title__container
    %h2.content-title パスワード再登録
    %p.content-lead
      登録済みのメールアドレスを入力してください。
      %br
      ご入力いただいたメールアドレス宛にパスワード再登録の案内メールをお送りします。
  .form__post-new
    = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
      = devise_error_messages!
      .form-item
        = f.label :email, class: "form-label"
        = f.email_field :email, autofocus: true, class: "form-control"
      .form-item
        = f.submit "送信", class: "button-default"
  %ul.shared__link-items
    %li.shared__link-item
      = link_to "ログイン", new_session_path(resource_name), class: 'text-link primary'
    %li.shared__link-item
      = link_to "新規登録", new_registration_path(resource_name), class: 'text-link primary'
